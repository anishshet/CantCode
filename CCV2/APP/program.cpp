#include<iostream>
using namespace std;
int main(int argc, char *argv[])
{
        int a,b,add;
        a = atoi(argv[1]);
        b = atoi(argv[2]);
        add = a+b;

        cout<<add;
        return 0;
}